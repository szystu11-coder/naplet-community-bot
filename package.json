{
  "name": "naplet-community-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch index.ts",
    "build": "tsc",
    "start:bot": "node dist/index.js",
    "start": "npm run deploy && npm run start:bot",
    "deploy": "tsx deploy-commands.ts",
    "setup-server": "tsx setup-server.ts",
    "publish-panels": "tsx publish-panels.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "discord.js": "^14.22.1",
    "dotenv": "^17.2.3",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
